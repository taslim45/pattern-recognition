SL SW PL PW are the characteristic features of three classes of objects.

The three classes are represented as 0, 1, and 2 for simplicity.

The Gaussian Naive Bayes formula is used to calculate the probability of test data.

http://en.wikipedia.org/wiki/Naive_Bayes_classifier

How the program works :
1. This is a C++ console program. 
2. set_training.txt is fed to the classifier as training data set.
3. set_test.txt is the testing data set.